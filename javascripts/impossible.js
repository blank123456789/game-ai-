function setup() {
  canvas = createCanvas(400,400);
  canvas.position(canvasx,canvasy);
  var canvasx = (windowWidth - width) / 2;
  var canvasy = ((windowHeight - height) / 2) + 100;
  background(255);
}

function draw() {
}
